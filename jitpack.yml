before_install:
  - git config --global user.name "Jitpack CI"
  - git config --global user.email "no-reply@github.com"
  - git submodule update --recursive --init
  - ./scripts/applyPatches.sh
install:
  - mvn -B clean install
